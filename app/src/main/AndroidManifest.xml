<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.motionlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotionLayout">
        <activity android:name=".KeyTriggerActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".TestKeyCycleActivity" />
        <activity android:name=".CreateMotionActivity" />
        <activity android:name=".TransitionActivity" />
        <activity android:name=".KeyCycleActivity" />
        <activity android:name=".KeyTimeCycleActivity" />
        <activity android:name=".ClickSwipeActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".KeyFramesActivity" />
        <activity
            android:name=".ConstraintSetActivity"
            android:launchMode="singleTop" />
        <activity android:name=".MainActivity">




        </activity>
    </application>

</manifest>